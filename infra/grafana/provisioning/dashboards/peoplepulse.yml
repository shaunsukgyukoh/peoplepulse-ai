apiVersion: 1

providers:
  - name: PeoplePulse
    orgId: 1
    folder: PeoplePulse
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
